#include <cstdio>
#include <cstring>
#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

const int N = 100005;
const int M = 200005;

int n, m, tot, dfc;
int dfn[N], low[N];

struct edge {
    int adj, id;
    edge() {}
    edge(int _adj, int _id) {
        adj = _adj;
        id = _id;
    }
};

vector<edge> G[N];

int main() {

    return 0;aaa;
}
